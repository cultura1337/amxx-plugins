#if defined _hns_xp_core_included
    #endinput
#endif
#define _hns_xp_core_included

native hns_xp_get_xp(id);
native hns_xp_get_level(id);
native hns_xp_get_playtime(id);
native hns_xp_add_xp(const id, const amount);
